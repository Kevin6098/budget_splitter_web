# Budget Splitter API - Environment Configuration
# Copy to .env and fill in values

# Mode: "local" (SQLite, no auth) or "vps" (PostgreSQL, auth)
MODE=local

# Server
PORT=3012

# --- VPS Mode (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=budget_splitter
DB_USER=budget_user
DB_PASSWORD=your_secure_password
DB_SSL=false

# JWT for auth (VPS mode only)
JWT_SECRET=your-very-long-random-secret-key-minimum-32-characters

# --- Local Mode (SQLite) ---
# Optional: custom path for SQLite file (default: ./data/budget_splitter.db)
# SQLITE_PATH=./data/budget_splitter.db
